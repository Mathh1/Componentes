import './styles.css';
import './Carrito.jpg';

const CartWidget = (props) => {
    return (
    <div className="carrito">
        <a href="#">
            <img src="./Carrito.jpg" />
        </a>
    </div>
    )
}

export default CartWidget